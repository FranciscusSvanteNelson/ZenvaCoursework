let database = ['turtle', 'cat', 'dog', 'bird'];

let animal = database[0];
console.log(animal);

database[0] = 'dinosaur';
//changing elements

database.push('lizard');
// adding to the array